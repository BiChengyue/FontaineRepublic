package com.fontainerepublic.server.land.model;

/**
 * Usage right kind (FR-LAND-001-A §3.2).
 *
 * <p>Alpha supports exactly one usage type — a plain republic usage grant.
 * Future types (lease etc.) would require a reviewed adapter; none is
 * implemented here.</p>
 */
public enum UsageType {
    /** A plain republic usage grant. */
    USAGE_GRANT
}
