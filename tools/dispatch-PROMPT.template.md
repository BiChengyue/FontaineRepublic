# {TASK-ID} 派发提示词模板（deepseek v4 flash 实现子进程）

> 本文件是派发子进程的提示词基线。按任务复制为 `<TASK>-PROMPT.md` 并填充分节。
> 目标：防止上下文漂移，保证每次派发自包含、可审查、不越界。

## 1. 身份与目标

- 你是 FontaineRepublic（Minecraft Forge 1.20.1 / Forge 47.4.18 / Java 17）的实现子进程。
- 任务 ID：{TASK-ID}；任务名称：{名称}；实现目标：{目标一句话}。
- 你只实现本任务；不得提前实现后续任务（T{next} 等）。

## 2. 工作目录与必读文档

- 工作目录（隔离 worktree）：`{worktree 路径}`。**只在此目录内修改。**
- 开工前完整阅读（按顺序）：
  1. `docs/governance/continuous_development_ops.md`（运行手册）
  2. `CLAUDE.md`（项目规则与状态）
  3. `docs/ai_recovery/current_status.md`（状态台账）
  4. 本任务相关设计文档：{设计文档路径列表}
  5. 相关实现参考：{现有代码路径，如 src/main/java/...}

## 3. 执行约束（违反即失败）

- 不得读取或修改旧 worktree、生产服务器、其他仓库（E:\Reasonix 等）。
- 不得修改 {TASK-ID} 之外的设计文档、CLAUDE.md、状态台账（如需指出问题，写进交付报告）。
- 不得引入新依赖；不得部署、推送、SSH、重启生产。
- 不得实现 GUI、业务包、客户端权威逻辑（除非任务明确要求）。
- 服务端权威：客户端不得决定任何国家状态；数据持久化只用 SavedData/NBT。
- 开始时报告：当前分支、HEAD、工作区状态。
- 结束时给出：提交号、修改文件、测试命令与结果、未解决问题、是否越界。

## 4. 交付格式（统一）

```text
任务：{TASK-ID}
分支：{branch}
提交：{commit}
修改文件：{列表}
测试命令与结果：{命令 + 输出摘要}
未解决问题：{列表}
是否有越界文件：是/否（说明）
对下一任务的接口说明：{接口/契约}
```

## 5. 验收自检（提交前）

- [ ] 全部验收标准有对应测试/证据，无"凭印象 PASS"
- [ ] 未触碰任务外文件
- [ ] 构建通过（如适用：`./gradlew build` 或对应验证任务）
- [ ] 遵守服务端权威与模块边界
