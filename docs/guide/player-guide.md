# 枫丹共和国 · 玩家上手指南

> 面向玩家（非技术）的入门说明。最后更新：2026-08-14。

---

## 一、这是什么

枫丹共和国 Mod 把服务器的"国家制度"搬进游戏：你登录后会自动获得
**公民身份**和**零余额账户**，之后可以查余额、互相转账、参与土地使用、
政府任职、议会立法与法院诉讼。所有数据由服务器权威保管，客户端只负责显示。

## 二、开始之前

- 安装：把 mod 放进 `mods/` 即可；不装客户端模组也能使用服务器命令与聊天功能。
  仅“我持有使用权的地块”个人列表属于手机专用展示，不提供无客户端命令。
- 第一件事：登录服务器，系统会自动为你开户（公民 + 账户）。

## 三、常用命令

所有命令以 `/fr` 开头：

| 命令 | 作用 |
|---|---|
| `/fr help` | 查看当前可用的命令分类 |
| `/fr money balance` | 查看我的余额 |
| `/fr money pay <玩家名或UUID或登记号> <金额> [备注]` | 给他人转账 |
| `/fr money history [页码]` | 查看我的收支记录 |
| `/fr citizen info` | 查看我的公民身份与政治等级 |
| `/fr court case list`、`/fr government position list` 等 | 查看各机构公开信息 |

公开登记号长这样：`10-000001-61`（水神个人）、`00-000001-95`（水神职位）。
你的个人登记号在开户后由服务器分配，可用于转账路由。

## 四、国家是怎么运转的

四大柱石（宪法框架）：

- **议会**：提案 → 投票 → 法案。不同层级的法律需要不同门槛
  （普通法过半数、组织法 2/3、宪法基本法 3/4）。
- **政府**：部门/职位/官员任命。任命与罢免是**现场职责**——
  必须到注册的政府设施终端前操作。
- **法院**：案件 → 证据 → 裁判；土地违规举报可自动立案；有复核路径。
- **中央银行**：国库与货币（现场职责，后续开放）。

**重要规则**：政治职位（如"水神"等级）不代表技术权限；重要操作必须在
对应机构实体场所现场完成，不能在聊天框里远程办公。

## 五、常见问题

- **我没装客户端模组能用吗？** 能使用全部服务器命令与聊天反馈；但“我持有
  使用权的地块”个人列表是手机专用展示，不提供无客户端命令。
- **转账给谁？** 支持玩家名 / UUID / 公开登记号三种方式；名字歧义时会提示
  "无法唯一解析"，不会转错人。
- **数据安全**：关键变更（开户、转账、任命、立法、裁判）都是确认式落盘，
  崩溃后不会"转账成功但丢了记录"。
- **更多帮助**：游戏内 `/fr help <模块>`；或咨询服务器管理员。

## 五.5 客户端模组（可选 FR Client，协议 v9）

带 FontaineRepublic 模组的 Forge 客户端连服即可获得可视化界面；没装模组
（或装的是无 FR 模组的 Forge 客户端）仍可使用服务器命令 + 聊天反馈；唯一例外
是“我持有使用权的地块”个人列表，它按 Human 决定仅在手机端提供。

如何运行 FR 客户端：

- **开发环境**：项目目录执行 `.\gradlew.bat runClient`（Dev 客户端自动加载
  本模组，与 `runServer` 同一机制）；
- **正式部署**：`.\gradlew.bat build` 后取 `build/libs/` 下的 jar，放入客户端
  `mods/`（需 Minecraft 1.20.1 + Forge 47.4.18）；
- **协议一致**：客户端与服务端必须为同一构建（当前协议 v9），版本不一致会在
  握手时被拒绝。

游戏内客户端命令：

- `/frclient` —— 主界面（余额卡入口）；
- `/frclient money` —— 余额卡 + 转账表单 + 实时交易流水；
- `/frclient citizen` —— 公民卡（登记号/状态/政治等级/入籍时间）；
- `/frclient history` —— 交易历史（登录后同步第一页）；
- `/frclient notifications` —— 待读通知；
- `/frclient government` —— 政府公开摘要（部门/职位数）；
- `/frclient parliament` —— 议会公开摘要（提案）；
- `/frclient court` —— 法院公开摘要（案件）；
- `/frclient land` —— 共和国土地概况（地块数/面积/分区分布）；
- `/frclient guide` —— 引导页（链接 `/fr help` 与本文档）。

手持**传讯水镜**打开土地界面后，还可查看“我持有使用权的地块”：

- 只显示当前仍然有效的个人用地权益，不显示已撤销或已过期记录；
- 列表支持分页与刷新，内容以服务器查询结果为准；
- 该个人权益列表仅供安装客户端模组并手持传讯水镜的玩家查看，**没有对应的
  无客户端命令**；
- 现有 `/fr land inspect <x> <y> <z>` 与 `/fr land claim <x> <y> <z>` 仍可正常
  使用，不受上述限制影响。

要点：

- 界面只做**展示与输入转发**：表单提交走 `/fr money pay` 等同一命令路径，
  服务器重新解析目标并校验金额/备注，失败仅展示服务器反馈；
- 客户端从不决定余额、权限、所有权或任何状态；显示缓存登出即清空；
- 余额/通知/交易流水由服务器在登录与变更后推送（无 FR 模组客户端收不到
  也不受影响）。

## 六、给管理员的提示

- 玩家引导入口：本文件 + `/fr help` 体系（持续完善中）。
- 真机验证清单见 `docs/development/LEVEL3-RUNTIME-VERIFICATION-CHECKLIST.md`。
